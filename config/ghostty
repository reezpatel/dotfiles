font-family = "GeistMono Nerd Font"
theme = "Github Dark"
cursor-style = block
cursor-click-to-move = true
cursor-style-blink = false
scrollback-limit = 1000000000

quick-terminal-position = top
quick-terminal-screen = macos-menu-bar
quick-terminal-autohide = false

keybind = global:super+`=toggle_quick_terminal
keybind = super+enter=unbind

macos-option-as-alt = left

keybind = alt+left=unbind
keybind = alt+right=unbind

macos-secure-input-indication = true

auto-update = download
auto-update-channel = stable

command = /bin/zsh -l -c "if tmux has-session 2>/dev/null; then tmux attach; else tmux new -s lab; fi"

window-height=45

font-feature = +liga  # Enable ligatureso


# Cell adjustment for line height equivalent to 1.6
# For a 1.6 line height, we need approximately 60% increase
adjust-cell-height = 15%

